# Copy this file to keystore.properties in the project root (gitignored).
# Fill in values before running: ./gradlew :app:bundleRelease
#
# Do not commit keystore.properties or .jks / .keystore files.

storeFile=release-upload.keystore
storePassword=
keyAlias=
keyPassword=
